[metadata]
id = "first-non-blank-08"
title = "Jump to First Non-Blank Character"
description = "Navigate to 'if' and change it to 'while'"
difficulty = "beginner"
tags = ["movement", "line-navigation", "change"]

[hints]
generic = "Move to the first non-whitespace character on the line"
helix = "Press '^' to jump to first non-blank character, then 'cw' to change word, type 'while', press Escape"
hint_1 = "The '^' command moves to the first non-whitespace character, skipping indentation"
hint_2 = "After '^' positions you on 'if', use 'cw' to change the word (deletes and enters insert mode)"
hint_3 = "Complete: '^' to 'if', 'cw' to change word, type 'while', Escape"
optimal_solution = "^cwwhile\x1b"
optimal_keystrokes = 9

[content]
starting = "    if (condition)"
target = "    while (condition)"
