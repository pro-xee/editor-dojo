[metadata]
id = "nullable-reference-45"
title = "Add Nullable Reference Types"
description = "Make all string parameters nullable"
difficulty = "beginner"
tags = ["csharp", "nullable", "types"]

[hints]
generic = "Add '?' after each 'string' type to make them nullable"
helix = "Use 'f' to find each 'g' at end of 'string', press 'a' to append, type '?', press Escape, repeat with ';' for next occurrence"

hint_1 = "Nullable reference types use '?' after the type name"
hint_2 = "Find first 'string', append '?', then repeat for second 'string'"
hint_3 = "Complete: 'ea?' for first string, 'ea?' again for second (use ';' to repeat find if needed)"
optimal_solution = "ea?wea?\x1b"
optimal_keystrokes = 8

[content]
starting = "public void Process(string firstName, string lastName)"
target = "public void Process(string? firstName, string? lastName)"
