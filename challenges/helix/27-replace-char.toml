[metadata]
id = "replace-char-27"
title = "Replace Single Character"
description = "Replace the '=' with '+=' to change assignment to addition assignment"
difficulty = "beginner"
tags = ["replace", "character", "modification"]

[hints]
generic = "Replace a single character without entering insert mode"
helix = "Move cursor to '=', press 'r' then '+' to replace with '+', move right and press 'a' to append '=', press Escape"
hint_1 = "The 'r' command replaces the character under cursor with the next character you type"
hint_2 = "You need to: replace '=' with '+', then add another '=' to make '+='"
hint_3 = "Complete: 'f=' to find '=', 'i' to insert, type '+', Escape (or 'f=r+a=', Escape)"
optimal_solution = "f=i+\x1b"
optimal_keystrokes = 5

[content]
starting = "counter = 1;"
target = "counter += 1;"
