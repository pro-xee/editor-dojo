[metadata]
id = "linq-method-48"
title = "Add LINQ Method Chain"
description = "Add '.Where(x => x.IsActive)' before .ToList()"
difficulty = "beginner"
tags = ["csharp", "linq", "method-chain"]

[hints]
generic = "Find the position before .ToList() and insert the Where clause"
helix = "Move to '.ToList', press 'i' to insert, type '.Where(x => x.IsActive)', press Escape"

hint_1 = "LINQ methods can be chained - each method returns an IEnumerable"
hint_2 = "Navigate to the '.' before 'ToList' and insert the Where clause before it"
hint_3 = "Complete: 'f.i' to find '.' and insert, type '.Where(x => x.IsActive)', Escape"
optimal_solution = "f.i.Where(x => x.IsActive)\x1b"
optimal_keystrokes = 29

[content]
starting = "var users = repository.GetAll().ToList();"
target = "var users = repository.GetAll().Where(x => x.IsActive).ToList();"
