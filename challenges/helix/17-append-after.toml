[metadata]
id = "append-after-17"
title = "Append After Cursor"
description = "Add '?' after 'string' to make it nullable"
difficulty = "beginner"
tags = ["insertion", "append", "basic"]

[hints]
generic = "Position cursor and append text after it"
helix = "Move cursor to 'g' in 'string', press 'a' to append, type '?', press Escape"
hint_1 = "The 'a' command enters insert mode after the cursor position"
hint_2 = "Use 'e' to move to the end of 'string' (the 'g'), then 'a' to append after it"
hint_3 = "Complete: 'e' to end of 'string', 'a' to append, type '?', Escape"
optimal_solution = "ea?\x1b"
optimal_keystrokes = 4

[content]
starting = "public string Name { get; set; }"
target = "public string? Name { get; set; }"
