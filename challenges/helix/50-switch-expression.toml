[metadata]
id = "switch-expression-50"
title = "Edit Switch Expression"
description = "Add '_ => \"Unknown\"' as the default case"
difficulty = "beginner"
tags = ["csharp", "switch", "expression", "pattern-matching"]

[hints]
generic = "Add a comma after the last case and add the default case"
helix = "Move to end of '\"Large\"', press 'a' to append, type ',', press 'o' to open line, type '    _ => \"Unknown\"', press Escape"

hint_1 = "Switch expressions need a default case using the discard pattern '_'"
hint_2 = "Add comma after last case, open new line, add default case with proper indentation"
hint_3 = "Complete: navigate to end of 'Large"', 'a,o' to append comma and open line, type '    _ => "Unknown"', Escape"
optimal_solution = "$i,o    _ => "Unknown"\x1b"
optimal_keystrokes = 21

[content]
starting = """var size = quantity switch {
    < 10 => "Small",
    < 100 => "Large"
};"""
target = """var size = quantity switch {
    < 10 => "Small",
    < 100 => "Large",
    _ => "Unknown"
};"""
