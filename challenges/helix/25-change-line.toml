[metadata]
id = "change-line-25"
title = "Change Entire Line"
description = "Replace the entire line with 'return true;'"
difficulty = "beginner"
tags = ["change", "line", "modification"]

[hints]
generic = "Delete the entire line and enter insert mode"
helix = "Position cursor on the line, press 'cc' to change line, type 'return true;', press Escape"
hint_1 = "In Helix, select the line with 'x' then 'c' to change, or use alt-c for change line"
hint_2 = "Navigate to line 2 using 'j' to move down, maintain indentation when typing"
hint_3 = "Complete: 'j' to line 2, 'xc' to change line, type '    return true;', Escape"
optimal_solution = "jxc    return true;\x1b"
optimal_keystrokes = 20

[content]
starting = """public bool IsValid() {
    return false;
}"""
target = """public bool IsValid() {
    return true;
}"""
